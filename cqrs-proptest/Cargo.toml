[package]
name = "cqrs-proptest"
version = "0.1.0"
authors = ["Marcus Griep <mgriep@vistaprint.com>"]
edition = "2018"

[dependencies]
cqrs-core = { path = "../cqrs-core" }
proptest = "0.8"

[dev-dependencies]
serde = {version = "1.0", features = ["derive"] }
serde_json = "1.0"